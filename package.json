{
  "name": "launchpad-skn",
  "version": "1.0.0",
  "description": "Caribbean Secondary School Learning Management System",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend/build && npx serve -s . -l $PORT",
    "heroku-postbuild": "cd frontend && npm install --legacy-peer-deps && npm run build"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ]
}

